# Personal Git Report

🌟 **个人 Git 年度报告生成器** - 为开发者打造精美的编程年度总结

## 📖 项目简介

Personal Git Report 是一个基于 TypeScript 开发的 Git 数据分析工具，能够深度分析你的 Git 仓库提交历史，自动生成包含丰富数据可视化的年度编程报告。

### ✨ 核心功能

- 📊 **全面的数据统计** - 提交次数、代码行数、活跃时段等多维度分析
- 📈 **可视化图表** - 语言分布、时间热力图、提交趋势等精美图表
- 🏆 **成就系统** - 自动解锁编程成就徽章
- 🎨 **多主题支持** - 暗黑/明亮/彩色三种主题可选
- 📱 **响应式设计** - 完美适配桌面和移动端
- 🔧 **灵活配置** - 支持配置文件和命令行参数

## 🚀 快速开始

### 环境要求

- **Node.js** >= 16.0.0
- **Git** >= 2.20.0

### 安装依赖

```bash
# 克隆项目
git clone https://github.com/yourusername/personalGitReport.git
cd personalGitReport

# 安装依赖
npm install
```

### 本地测试

#### 方式一：开发模式（推荐）

```bash
npm run dev
```

这会使用 `ts-node` 直接运行 TypeScript 源码，无需编译，适合快速测试。

#### 方式二：构建后运行

```bash
# 构建项目
npm run build

# 运行编译后的代码
npm start
```

#### 方式三：使用 CLI 命令

```bash
# 构建后，可以使用 CLI 命令
node dist/cli/index.js

# 指定年份
node dist/cli/index.js --year 2024

# 指定输出目录
node dist/cli/index.js --output ./my-reports

# 选择主题
node dist/cli/index.js --theme dark
```

## 📋 命令行使用

### 基础命令

```bash
# 分析当前目录的 Git 仓库
git-report

# 分析指定路径的仓库
git-report /path/to/your/repo

# 指定年份（默认当前年份）
git-report --year 2024

# 选择主题
git-report --theme dark    # dark | light | colorful

# 指定输出格式
git-report --format html   # html | json | pdf

# 指定输出目录
git-report --output ./reports

# 指定作者邮箱（过滤提交）
git-report --author your-email@example.com
```

### 多项目分析

```bash
# 分析多个项目
git-report multi --projects "/path/to/proj1,/path/to/proj2,/path/to/proj3"

# 多项目 + 指定年份
git-report multi --projects "/proj1,/proj2" --year 2024
```

### 配置文件管理

```bash
# 生成示例配置文件
git-report init

# 使用指定配置文件
git-report --config ./my-config.js
```

## ⚙️ 配置文件

### 创建配置文件

运行 `git-report init` 会在当前目录生成 `git-report.config.js`：

```javascript
module.exports = {
  // 基本信息
  author: "你的名字",
  email: "your-email@example.com",

  // 主题设置
  theme: "dark",  // 'light' | 'dark' | 'colorful'

  // 显示选项
  showPrivateRepos: false,
  excludeWeekends: true,

  // 输出设置
  output: "./reports",
  format: ["html", "json"],  // 可同时输出多种格式

  // 分析选项
  includeMerges: false,  // 是否包含 merge 提交
  excludePaths: [        // 排除的文件路径
    "node_modules",
    "*.min.js",
    "dist",
    "build"
  ],

  // 自定义时间范围（可选）
  dateRange: {
    from: "2024-01-01",
    to: "2024-12-31"
  },

  // 排除的提交信息模式
  exclude: {
    commits: [
      "Merge pull request",
      "Update dependencies"
    ]
  },

  // 自定义统计选项
  customStats: {
    countLines: true,
    analyzeComplexity: false,
    trackLearning: true
  }
};
```

### 配置文件查找顺序

工具会按以下顺序查找配置文件：

1. 命令行指定的 `--config` 路径
2. `git-report.config.js`
3. `git-report.config.json`
4. `.git-report.json`

## 📊 报告内容

生成的报告包含以下内容：

### 📈 统计数据

- **提交统计** - 总提交数、新增/删除代码行数、净增长
- **连续记录** - 最长连续提交天数、当前连续天数、总活跃天数
- **语言分布** - 各编程语言使用占比
- **时间模式** - 按小时、星期、月份的提交分布
- **项目统计** - 各项目的提交数和代码量

### 📉 可视化图表

- **语言分布图** - 饼图展示各语言占比
- **提交趋势图** - 月度/日度提交趋势
- **时间热力图** - 24小时 × 7天的提交热力图
- **活跃时段图** - 按小时统计的活跃度

### 🏆 成就系统

自动解锁的成就徽章，包括：

- 🔥 **代码战士** - 提交数达到里程碑
- 🌙 **夜猫子** - 深夜提交占比高
- ⚡ **闪电侠** - 单日提交数记录
- 🎯 **连续打卡** - 连续提交天数
- 📚 **多语言大师** - 使用多种编程语言
- 🚀 **效率之王** - 代码产出效率高

### 🎭 开发者画像

根据你的编程习惯生成个性化标签，例如：

- 🌙 **午夜程序员** - 深夜最活跃
- ☀️ **早起鸟** - 清晨高效工作
- 🔥 **全栈战士** - 前后端通吃
- 🎨 **前端艺术家** - 专注前端开发

## 🛠 开发指南

### 项目结构

```
personalGitReport/
├── src/
│   ├── analyzer/          # Git 数据分析器
│   │   └── GitAnalyzer.ts
│   ├── report/            # 报告生成器
│   │   └── ReportGenerator.ts
│   ├── utils/             # 工具类
│   │   └── ConfigManager.ts
│   ├── cli/               # 命令行工具
│   │   └── index.ts
│   ├── templates/         # Handlebars 模板
│   │   ├── report_fixed.hbs
│   │   └── style.hbs
│   └── index.ts           # 主入口
├── dist/                  # 编译输出目录
├── docs/                  # 文档和示例
└── package.json
```

### 核心模块说明

#### 1. GitAnalyzer（Git 分析器）

负责从 Git 仓库提取和分析数据：

- 使用 `simple-git` 库获取提交历史
- 使用 `git log --numstat` 优化性能，避免 N+1 查询
- 分析语言分布、时间模式、连续记录等
- 计算成就和开发者画像

#### 2. ReportGenerator（报告生成器）

负责生成可视化报告：

- 使用 Handlebars 模板引擎
- 支持 HTML、JSON、PDF 三种输出格式
- 使用 Puppeteer 生成 PDF
- 内置响应式 CSS 样式

#### 3. ConfigManager（配置管理器）

负责配置文件的加载和验证：

- 支持 JS 和 JSON 格式配置文件
- 配置验证和默认值合并
- 年份参数解析

### 可用脚本

```bash
# 开发模式（使用 ts-node）
npm run dev

# 构建项目
npm run build

# 运行编译后的代码
npm start

# 运行测试
npm test

# 监听模式运行测试
npm run test:watch

# 代码检查
npm run lint

# 自动修复代码问题
npm run lint:fix

# 代码格式化
npm run format
```

### 添加新功能

1. **添加新的分析维度**：在 `GitAnalyzer.ts` 中添加新的分析方法
2. **添加新的图表**：在 `report_fixed.hbs` 模板中添加图表配置
3. **添加新的成就**：在 `calculateAchievements` 方法中添加成就逻辑
4. **自定义样式**：修改 `style.hbs` 模板

## 🧪 测试

```bash
# 运行所有测试
npm test

# 监听模式
npm run test:watch

# 测试覆盖率
npm test -- --coverage
```

## 📝 常见问题

### Q: 如何只分析某个作者的提交？

A: 使用 `--author` 参数指定邮箱：

```bash
git-report --author your-email@example.com
```

### Q: 如何排除某些文件或目录？

A: 在配置文件中设置 `excludePaths`：

```javascript
excludePaths: ["node_modules", "dist", "*.min.js"]
```

### Q: 生成的报告在哪里？

A: 默认在 `./reports` 目录，可通过 `--output` 参数或配置文件修改。

### Q: 支持哪些输出格式？

A: 支持 HTML、JSON、PDF 三种格式，可在配置文件中同时指定多种格式。

### Q: 如何自定义时间范围？

A: 在配置文件中设置 `dateRange`：

```javascript
dateRange: {
  from: "2024-01-01",
  to: "2024-06-30"
}
```

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 提交 Pull Request

## 📄 开源协议

[MIT License](LICENSE)

## 🙏 致谢

本项目使用了以下优秀的开源库：

- [simple-git](https://github.com/steveukx/git-js) - Git 操作
- [commander](https://github.com/tj/commander.js) - CLI 框架
- [handlebars](https://handlebarsjs.com/) - 模板引擎
- [chart.js](https://www.chartjs.org/) - 图表库
- [puppeteer](https://pptr.dev/) - PDF 生成
- [inquirer](https://github.com/SBoudrias/Inquirer.js) - 交互式命令行
- [chalk](https://github.com/chalk/chalk) - 终端样式
- [ora](https://github.com/sindresorhus/ora) - 加载动画

---

<div align="center">

**🌟 如果这个工具对你有帮助，请给个 Star 支持一下！**

Made with ❤️ by developers, for developers

</div>